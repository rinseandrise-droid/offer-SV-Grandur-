# Local dev — optional PostgreSQL (otherwise uses data/sv-grandur.db)
# On Railway: use Variable Reference from Postgres service (do not paste internal URL manually)
DATABASE_URL=postgresql://postgres:PASSWORD@HOST.proxy.rlwy.net:PORT/railway

SV_GRANDUR_ADMIN_PASSWORD=your-secure-password
OFFER_VALID_UNTIL=30/09/2026
